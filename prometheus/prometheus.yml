global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "identityforge"

    metrics_path: /system/metrics

    static_configs:
      - targets:
          - "backend:5000"